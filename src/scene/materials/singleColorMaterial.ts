import { Material } from "./material.js";

/**
 * single color material, for debug or information drawing.
 */
export class SingleColorMaterial extends Material {
    public constructor() {
        super();
    }
}