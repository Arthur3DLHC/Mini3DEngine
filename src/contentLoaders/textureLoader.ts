export class TextureLoader {
    // load texture from url, call callback function when loaded
    // firstly find in resource cache.
    // after loaded, put to cache.

    // method for clear texture cache.

    // or hold the texture cache inside this class?
}